const http = require('http');
const https = require('https');
const fs = require('fs');
const path = require('path');
const os = require('os');
const events = require('events');
const util = require('util');

http
https
fs
path
os
events
util



this
const http = require('http');
means http in ('http') is global module

this
const http = require('./http');
means ./http in ('./http') is a file named as http.js in same folder

